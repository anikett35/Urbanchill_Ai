// Example store structure - replace with your preferred state management (Zustand, Redux, etc.)
// import { create } from 'zustand';

export const useAppStore = () => {
  return {
    // State and actions go here
    user: null,
  };
};
